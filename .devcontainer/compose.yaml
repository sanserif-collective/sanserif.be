name: sanserif-devcontainer

networks:
  default:
    name: ${COMPOSE_PROJECT_NAME}-net

volumes:
  s3-data:
    name: ${COMPOSE_PROJECT_NAME}-s3-data
  strapi-database-data:
    name: ${COMPOSE_PROJECT_NAME}-strapi-database-data

services:
  s3:
    container_name: ${COMPOSE_PROJECT_NAME}-s3
    image: localstack/localstack
    environment:
      SERVICES: s3
      PERSISTENCE: 1
    volumes:
      - s3-data:/var/lib/localstack

  strapi-database:
    container_name: ${COMPOSE_PROJECT_NAME}-strapi-database
    image: postgres:14-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    volumes:
      - strapi-database-data:/var/lib/postgresql/data

  workspace:
    container_name: ${COMPOSE_PROJECT_NAME}-workspace
    image: node:20-alpine
    command: sleep infinity
    depends_on:
      - s3
      - strapi-database
    volumes:
      - ../.:/workspace:cached
